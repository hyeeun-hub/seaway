# orchestrator.md - ERP Flow Harness 실행 흐름 연결 책임

이 문서는 ERP Flow Harness에서 파일 업로드부터 최종 결과 확인까지
중간에 다시 묻지 않고 끝까지 이어지도록 만드는 실행 연결 책임을 정리한 문서다.

오케스트레이터는 개별 단계를 대신 처리하는 역할이 아니라,
각 단계가 끝나면 다음 단계로 자동으로 이어지게 만드는 흐름 연결 중심이다.
예외는 마지막 final-assembly 단계로, 이것만은 오케스트레이터가 직접 수행한다(§7).

노출 범위는 `SPEC.md` §3 결과 노출 정책을 기준으로 한다. 이 문서에서 노출 정책을 다시 정의하지 않는다.

---

## 1. 역할

오케스트레이터는 아래 역할을 담당한다.

- 업로드 + 명령 이후 전체 흐름을 자동으로 시작하고 끝까지 이어간다.
- file-input → analyze → classify → query-export → calendar-chatbot → verify → final-assembly → 최종 결과 순서를 관리한다.
- 한 단계가 끝나면 재요청이나 중간 확인 없이 바로 다음 단계를 시작한다.
- 특히 analyze가 끝나면 중간 확인 없이 바로 classify로 넘어가 검수 대상을 정리한다.
- final-assembly에서 앞 단계 결과를 모아 최종 결과를 8개 항목으로 한 번에 정리해 웹 화면(webapp)에 보여준다.
- 진행 표시는 `SPEC.md` §3 범위 안에서 하되, 흐름 자체는 끊기지 않게 이어간다.

즉, 오케스트레이터는
“각 단계를 잘 아는 역할”이 아니라
“단계들을 순서대로 붙여서 끝까지 가게 하는 역할”이다.

---

## 2. 시작 조건

- ERP 파일 업로드와 분석 요청이 들어오면 오케스트레이터가 흐름을 시작한다.
- 첫 명령 이후에는 사용자에게 다시 묻거나 재요청하지 않고, 앞 단계 완료 기준으로 다음 단계를 계속 이어 실행한다.
- 한 번 시작된 흐름은 file-input부터 final-assembly까지 이어서 진행한다.
- 조회/생성 요청, 챗봇 질의, 캘린더 수정 입력처럼 별도 입력이 필요한 지점은 해당 시점에만 별도 트리거로 다룬다.

---

## 3. 실행 순서

오케스트레이터가 관리하는 실행 순서는 다음과 같다.

1. 파일 업로드
2. file-input-agent: 파일 접수, 저장/정리, 업로드 시점 기록, 중복 여부 확인
3. analyze-agent: 파싱, 정규화, 프로젝트/현장 매칭, 손익 계산, 월별 집계, 일반관리비 분류
4. classify-agent: 불확실/확인 필요 거래 분리, 문제 유형/처리 방법 정리, 확정/제외/수정/보류 구분, 검수 목록 생성
5. query-export-agent: 프로젝트별 손익, 월별 손익, 월간 리포트 조회/생성용 결과 준비
6. calendar-chatbot-agent: 회수 캘린더, 챗봇 응답, 실행 요약/상태 정리
7. verify-agent: 파일/분석/검수/결과 단계 자동 검사, 최종 무결성 점검, 이상 시 검수 연결
8. final-assembly(오케스트레이터 직접 수행): 앞 단계 결과를 모두 모아 최종 결과 형식으로 정리, 웹 화면(webapp)에 표시
9. 최종 결과 출력: 8개 항목을 한 번에 보여준다.

verify는 8단계 흐름 안의 7번 자리에만 있는 것이 아니라, `SPEC.md` §6이 정의한 6개 지점
(`after_file_input`, `after_analyze`, `after_classify`, `after_query_export`, `after_calendar_chatbot`, `after_final_result`)에서 반복 실행된다.

---

## 4. 단계별 연결 규칙

각 단계는 끝나면 자기 결과를 다음 단계로 넘긴다.

- file-input이 끝나면 analyze로 넘긴다.
- analyze가 끝나면 중간 확인 없이 바로 classify로 넘긴다.
- classify가 끝나면 query-export로 넘긴다.
- query-export가 끝나면 calendar-chatbot으로 넘긴다.
- calendar-chatbot이 끝나면 verify로 넘긴다.
- verify가 끝나면 final-assembly로 넘긴다.
- final-assembly에서는 앞 단계 결과를 모아 최종 결과로 정리한다. 흐름의 마지막 연결이다.

연결 방식은 아래 기준을 기본으로 한다.

- 단계별로 결과를 따로 저장했다가 다시 읽는 방식보다, 순서대로 넘겨서 바로 이어지게 처리한다.
- 예외는 거래 누적 보관과 파일 처리 기록이다. 이 둘은 실행 사이에 유지돼야 하므로 `state-schema.md`가 정의한 상태 파일에 저장한다.
- 중간 결과가 필요하면 다음 단계가 바로 이어받아 쓸 수 있게 남긴다.
- 한 단계가 끝나면 “끝”이 아니라 “다음 단계 시작”으로 이어지게 한다.

---

## 5. 중간 멈춤/재요청 처리 방식

오케스트레이터는 중간에 멈추는 방향을 기본으로 하지 않는다.

- 한 단계가 끝나면 재요청이나 중간 확인 없이 다음 단계로 넘어간다.
- 사용자가 다시 말을 해야 이어지는 방식은 이 흐름의 기본 동작이 아니다.
- 다만 다음 경우에는 예외로 다룬다.
  - 사용자 입력이 실제로 필요한 지점
  - 파일 읽기 실패, 필수 열 누락, 파싱/정규화 이상처럼 접수/분석 자체가 성립하지 않는 경우
  - 검증 이상이 발생해서 검수/분류 단계로 넘겨야 하는 경우

이 예외 상황에서도 흐름은 가능하면 끊기지 않게 처리하고,
필요하면 검수/재시도/안내 쪽으로 연결한다.

---

## 6. 중간 결과 전달 방식

오케스트레이터는 각 단계의 결과를 다음 단계로 이어받을 수 있게 전달한다.

- 각 단계는 자기 결과를 정리된 형태로 남긴다.
- 오케스트레이터는 그 결과를 다음 단계로 바로 넘겨서 이어지게 한다.
- final-assembly는 앞 단계 결과를 필요한 만큼 받아 최종 결과로 묶는다.

전달할 때 넣는 기본 정보는 다음과 같다.

- source: 어떤 단계에서 넘어온 결과인지
- timestamp: 처리 시점
- 결과 데이터: 각 단계가 맡은 산출물
- 이상/주의/검수 대상 정보: 있으면 함께 넘겨서 뒤에서 이어서 처리하게 한다

---

## 7. final-assembly 단계 책임

final-assembly는 오케스트레이터 흐름의 마지막 묶음이며, **오케스트레이터가 직접 수행한다.**
별도 에이전트나 스킬을 두지 않는다. 6개 에이전트는 자기 결과를 넘기는 것까지가 책임이다.

- file-input, analyze, classify, query-export, calendar-chatbot, verify 결과를 모아 최종 결과로 정리한다.
- 최종 결과는 웹 화면(webapp)에서 매 요청마다 다시 계산해 보여준다. 정적 파일을 새로 만들지 않는다.
- 웹 화면 구현의 세부 규칙은 `webapp/README.md`가 기준이다. 이 문서에서 다시 정의하지 않는다.
- 최종 결과에는 아래 8개 항목이 한 번에 포함된다.
  1. 파일 접수 요약
  2. 프로젝트별 손익
  3. 월별 손익 + 월간 리포트
  4. 일반관리비 분류 결과
  5. 검수 필요 목록 + 검수 목록 이동 표시 + 대표 예시
  6. 회수 캘린더/실행 요약
  7. 검사 결과/이상 항목/원인/조치 안내/재시도·안내 필요 여부
  8. 다음 액션 안내

final-assembly는 중간 계산 과정이나 단계별 세부 흔적을 길게 드러내지 않고,
앞 단계 결과를 한눈에 보기 좋게 묶는 데 집중한다. 이것이 흐름의 마지막 단계다.

---

## 8. 중간 표시 방식

기준은 `SPEC.md` §3 결과 노출 정책이다. 단계명 + 진행 상태 + 핵심 수치 1~2개, 단계당 최대 2줄.

예를 들면 이런 식이다.

- `파일 접수 완료 — 4개 파일, 869행`
- `분석 완료 — 신규 거래 869건 추가, 중복 0건 스킵`
- `검수 분류 완료 — 자동 처리 808건 / 확인 필요 57건`   ※ 건수는 형식 예시
- `결과 정리 완료 — 5개월, 프로젝트 22건`
- `검사 완료 — 이상 3건(금액 누락 1, 미매칭 2)`
- `최종 결과 정리 완료`

중간 표시는 흐름을 끊는 목적이 아니라, 현재 어디까지 왔는지를 짧게 알려주는 용도로만 쓴다.
거래 단위 상세, 중간 계산 과정, 규칙 판정 근거는 여기서 노출하지 않는다.

---

## 9. 문서/실행에서 맡는 범위

이 문서에서 정의하는 오케스트레이터는 다음 범위를 담당한다.

- 전체 흐름 시작
- 단계 간 자동 연결
- 중간 멈춤/재요청 없이 순차 진행하도록 만드는 책임
- final-assembly 수행과 최종 결과 출력 책임

다음 범위는 오케스트레이터가 직접 대신 처리하기보다,
해당 단계의 에이전트/스킬이 자기 역할로 수행한다.

- 파일 접수 세부 처리
- 파싱/정규화/집계/매칭 세부 처리(열 매핑과 중복 판정 규칙은 `state-schema.md`)
- 검수 분리/문제 유형/처리 방법 세부 처리
- 프로젝트별/월별/월간 리포트 세부 정리
- 캘린더/챗봇/실행 요약 세부 처리
- 검사/이상 원인/조치 안내 세부 처리

즉, 오케스트레이터는
“무엇을 처리하는가”보다
“언제, 어떤 순서로, 끊기지 않게 이어가게 하는가”를 맡는다.

---

## 10. 재현 기준

이 Harness를 다시 실행할 때도 아래 기준이 유지돼야 한다.

- 업로드 + 명령 한 번으로 흐름이 시작된다.
- 첫 명령 이후에는 다시 묻지 않고 끝까지 이어진다.
- analyze가 끝나면 바로 classify로 넘어간다.
- final-assembly에서 앞 단계 결과가 최종 결과 8개 항목으로 정리되어 웹 화면(webapp)에 나온다.
- 진행 표시와 최종 노출은 `SPEC.md` §3 기준을 따른다.

이 기준이 유지되면,
같은 업로드/명령 조건에서 비슷한 흐름으로 다시 실행할 수 있어야 한다.
