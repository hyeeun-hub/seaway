# state-schema.md - ERP Flow Harness 상태 파일 및 정규화 규칙

이 문서는 ERP Flow Harness가 실행 사이에 유지하는 상태 파일의 스키마, 거래 정규화 규칙, 중복 판정 알고리즘을 정의한다.

**이 문서는 다음 항목의 단일 기준이다.** 다른 문서는 이 문서를 참조하고 자체 정의를 두지 않는다.

- 거래 레코드 필드 정의 및 파일별 열 매핑
- 중복 판정 키(`tx_key`, `seq`)와 판정 알고리즘
- `transactions.json` / `processed_files.json` 스키마
- 회수/결제 예정일 파싱 규칙

---

## 1. 상태 저장소

이 문서가 정의하는 두 논리적 스키마는 `transactions.json`(거래 누적 보관)과
`processed_files.json`(파일 단위 처리 기록, 재처리 방지)이다. 이름은 원 설계의 파일명을
그대로 쓰지만, **실제 구현체(`webapp/`)에서는 JSON 파일이 아니라 Postgres 테이블로
존재한다** — `transactions.json` → `Transaction` 테이블(§8), `processed_files.json` →
`ProcessedFile` 테이블(§7, `webapp/prisma/schema.prisma`). analyze 단계(웹앱에서는
`lib/pipeline/ingest.ts`)가 쓰고, 다른 단계는 읽기만 한다. append-only 원칙과 아래
필드 정의는 저장소가 파일이든 DB든 동일하게 적용된다.

---

## 2. 거래 레코드 정의

정규화된 거래 레코드는 아래 필드를 가진다.

| 필드 | 타입 | 설명 | 비어 있을 수 있나 |
|---|---|---|---|
| `tx_key` | string | 중복 판정 해시. §4 참조 | 아니오 |
| `seq` | int | 동일 `tx_key` 내 출현 순번(1부터). §4 참조 | 아니오 |
| `kind` | string | 거래 종류. §3의 4개 값 중 하나 | 아니오 |
| `side` | string | `매출` 또는 `매입`. `kind`에서 파생 | 아니오 |
| `date` | string | 거래일자. `YYYY-MM-DD` | 아니오 |
| `month` | string | `YYYY-MM`. `date`에서 파생 | 아니오 |
| `place` | string | 거래처/사용처/세목명 | 아니오 |
| `amount` | int | 원 단위 정수 | 예(§5 참조) |
| `proj` | string | 프로젝트/현장 원문 | 예 |
| `use` | string | 용도 원문 | 예 |
| `content` | string | 내용 원문 | 예 |
| `evidence` | string | 증빙 구분(`세계`/`지방세` 등) | 예 |
| `payer` | string | 결제 수단/사용자(간이영수증의 카드사) | 예 |
| `settle_date` | string \| null | 회수/결제 예정일 `YYYY-MM-DD`. §6 참조 | 예 |
| `settle_method` | string \| null | 회수/결제 수단(`계좌` 등). §6 참조 | 예 |
| `source_file` | string | 원본 파일명. **참조용이며 중복 판정에 쓰지 않는다** | 아니오 |
| `source_row` | int | 원본 시트 행 번호(0-based) | 아니오 |
| `file_hash` | string | 원본 파일 해시 | 아니오 |
| `ingested_at` | string | 누적 보관에 추가된 시점(ISO 8601) | 아니오 |

---

## 3. 파일별 열 매핑

ERP 엑셀 4종은 모두 **0행 제목 / 1행 헤더 / 2행부터 데이터** 구조를 가지며, 열 이름만 다르다.
접수/파싱 단계가 아래 표로 열 차이를 흡수하고, 이후 단계는 §2의 공통 레코드만 다룬다.

| `kind` | 파일명 패턴 | `date` | `place` | `amount` | `evidence` | `payer` |
|---|---|---|---|---|---|---|
| `매출-세금계산서` | `매출_세금계산서_*.xls` | 작성일자 | 공급받는자상호 | 합계금액 | 증빙 | — |
| `매입-세금계산서` | `매입(세금계산서)_*.xls` | 작성일자 | 공급자상호 | 합계금액 | 증빙 | — |
| `매입-간이영수증` | `매입(간이영수증)_*.xls` | 사용일시 | 사용처 | 사용금액 | — | 사용자 |
| `매입-국세지방세` | `매입(국세_지방세)_*.xls` | 과세월일 | 세목명 | 총납부세액 | 증빙 | — |

`use`(용도), `content`(내용), `proj`(프로젝트/현장), `settle_*`(비고)은 4종 모두 동일한 열 이름을 쓴다.

`side`는 `kind`에서 파생한다. `매출-*` → `매출`, `매입-*` → `매입`.

### 열 이름이 표와 다를 때

헤더행에서 표의 열 이름을 찾지 못하면 **추측하지 않는다.** 접수 이상 항목으로 남기고 verify로 넘긴다.
새 파일 형식을 지원하려면 이 표에 행을 추가한다.

### 실데이터의 결측 분포(2026년 1~5월 기준)

정규화 규칙을 검증할 때 참고한다. 아래는 이상이 아니라 이 데이터의 정상 상태다.

| `kind` | 건수 | 주요 결측 |
|---|---|---|
| `매출-세금계산서` | 11 | 없음 |
| `매입-세금계산서` | 141 | `use` 5건 공백 |
| `매입-간이영수증` | 712 | `use` 709건 공백, `content` 전량 공백, `proj` 5건 공백 |
| `매입-국세지방세` | 5 | `proj` 전량 공백, `amount` 1건 공백 |

`매입-간이영수증`은 `use`/`content`가 사실상 비어 있으므로, 일반관리비 분류는 `place`(사용처) 문자열에 의존해야 한다.

---

## 4. 중복 판정

### tx_key

```
tx_key = sha256( kind + "|" + date + "|" + place + "|" + amount + "|" + proj + "|" + use + "|" + content )
```

- 각 값은 §5 정규화를 거친 뒤 결합한다. `amount`는 정수를 10진 문자열로 쓴다. 빈 값은 빈 문자열.
- **`source_file`은 키에 넣지 않는다.** 같은 거래가 재추출된 파일(`_2026년1-5월` → `_2026년1-6월`)로 다시 들어오면 파일명이 달라지므로, 키에 포함하면 중복 제외가 동작하지 않는다.
- **`kind`는 키에 넣는다.** 같은 날 같은 금액 같은 프로젝트의 매출/매입 쌍이 실재한다(2026-04-08 `창조이엔지` 21,489,886원 통과거래). `kind` 없이는 두 건이 하나로 합쳐질 위험이 있다.
- **`place`는 키에 넣는다.** 거래처가 다르면 다른 거래다.

### seq — 왜 필요한가

**`tx_key`만으로는 중복 판정이 불가능하다.** 실데이터에 모든 열이 완전히 동일한 정상 거래가 존재한다.

| 사례 | 건수 | 성격 |
|---|---|---|
| 2026-02-12 아시아나항공 85,400원 / 여수 신북항 / 하나카드 | 3 | 출장자 3명분 항공권. 10개 열 전부 동일 |
| 2026-03-11 한국도로공사 3,200원 / 추자 예초 | 2 | 왕복 통행료 |
| 2026-03-11 SOL페이_티머니_지하철 1,550원 | 2 | 왕복 교통비 |
| 컴포즈커피 1,500원(여러 날짜) | 각 2 | 커피 2잔 개별 결제 |

`사용자`(카드사)나 `작성일자` 열을 키에 추가해도 이 충돌은 줄어들지 않는다(실측: 삼켜지는 건수 10건에서 변화 없음).
필드만으로 "진짜 중복"과 "정상 반복"을 구분할 수 없으므로, **동일 `tx_key`의 출현 순번을 키의 일부로 삼는다.**

```
seq = 해당 파일 안에서 같은 tx_key가 나타난 순번(원본 행 순서 기준, 1부터)
거래 식별자 = (tx_key, seq)
```

같은 파일을 다시 업로드하면 같은 `(tx_key, seq)` 쌍이 생성되므로 전량 스킵된다.
정상 반복 3건은 `seq` 1/2/3으로 모두 살아남는다.

### 2단 방어선

중복 차단은 두 단계로 한다.

1. **파일 단위 — 1차 방어선.** `processed_files.json`의 `file_hash`가 이미 있으면 그 파일은 파싱하지 않고 스킵한다. 정상 재업로드는 여기서 전부 걸린다.
2. **거래 단위 — 2차 방어선.** 해시가 다른 파일(재추출, 기간 확장, 부분 수정)에 대해 `(tx_key, seq)`가 이미 누적돼 있으면 그 거래만 스킵한다.

### 판정 결과 기록

analyze 단계는 매 실행마다 아래 3개 수치를 남기고 최종 결과에 포함한다.

- `tx_added` — 새로 추가된 거래 건수
- `tx_skipped_duplicate` — 중복으로 스킵된 건수
- `tx_total` — 스킵 후 전체 누적 건수

### 알려진 한계

`(tx_key, seq)` 방식은 파일 안의 **행 순서가 안정적일 때만** 정확하다. ERP 추출은 거래일자 내림차순 고정이므로 실용상 안정적이지만, 정렬이 바뀐 재추출 파일에서는 동일 `tx_key` 그룹 내 `seq` 배정이 흔들릴 수 있다. 이 경우 중복이 삼켜지지 않고 **과다 반영**되는 방향으로 실패하므로, verify의 월별 집계 합계 검사가 이를 잡는다. 조용히 누락되는 방향으로는 실패하지 않는다.

---

## 5. 정규화 규칙

| 필드 | 규칙 |
|---|---|
| 문자열 전체 | 유니코드 NFC 정규화, 양끝 공백 제거. 파일명이 NFD로 저장되는 경우가 있으므로 필수 |
| `date` | `YYYY-MM-DD`로 변환. 시각이 붙어 있으면(`2026-06-02 08:34`) 날짜만 취한다 |
| `month` | `date[0:7]` |
| `amount` | 콤마/공백/통화기호 제거 후 정수 변환. **변환 실패 또는 공백이면 `null`로 두고 0으로 채우지 않는다.** classify가 `금액 누락`으로 분리한다 |
| `proj`/`use`/`content` | 원문 유지. 공백은 빈 문자열. **값을 추론해 채우지 않는다** |

값을 임의로 보정하지 않는 것이 원칙이다. 비어 있으면 비어 있는 상태로 넘기고, 판단은 classify가 한다.

---

## 6. 회수/결제 예정일 파싱 (`비고` 열)

`비고` 열은 자유 텍스트가 아니라 **회수/결제 예정일**을 담고 있다. 회수 캘린더의 유일한 데이터 소스다.

```
형식: "YYYY-MM-DD <수단>"     예: "2026-06-11 계좌"
```

| 항목 | 규칙 |
|---|---|
| `settle_date` | 앞 10자가 `YYYY-MM-DD`로 파싱되면 그 값 |
| `settle_method` | 날짜 뒤 나머지 토큰(`계좌` 등) |
| 형식 불일치 | `settle_date = null`, 원문은 `비고`로 보존. 캘린더에서 제외하고 classify가 `예정일 확인 필요`로 분리 |
| 공백 | `settle_date = null`. 이상이 아니다(§아래 분포 참조) |

### 실데이터 분포(2026년 1~5월)

| `kind` | 채워짐 | 공백 |
|---|---|---|
| `매출-세금계산서` | 11 / 11 | 0 |
| `매입-세금계산서` | 89 / 141 | 52 |

`매출`은 전량 채워져 있으므로 **회수 캘린더는 매출 11건 기준으로 완전하게 구성할 수 있다.**
`매입` 공백 52건은 현금/카드 즉시결제로 보이며, 예정 항목이 아니라 발생 항목으로 다룬다.

### 확정 / 예정 구분

`calendar-chatbot`의 "확정 계약 / 예정 계약"은 아래 기준으로 판정한다.

- `settle_date <= 오늘` → **확정**(회수 완료로 간주)
- `settle_date > 오늘` → **예정**
- `settle_date == null` → 캘린더 비대상

---

## 7. processed_files.json

**구현 매핑**: `webapp/prisma/schema.prisma`의 `ProcessedFile` 테이블로 구현돼 있다. 필드 정의는
아래가 그대로 기준이고, 저장소만 JSON 파일 대신 Postgres 테이블이다.

```json
{
  "version": 1,
  "files": [
    {
      "file_hash": "sha256:3f9a…",
      "file_name": "매출_세금계산서_2026년1-5월.xls",
      "source_path": "data/매출_세금계산서_2026년1-5월.xls",
      "size_bytes": 27648,
      "kind": "매출-세금계산서",
      "sheet": "sheet1",
      "header_row": 1,
      "data_start_row": 2,
      "row_count": 11,
      "uploaded_at": "2026-08-06T14:20:03+09:00",
      "processed_at": "2026-08-06T14:20:11+09:00",
      "tx_added": 11,
      "tx_skipped_duplicate": 0,
      "status": "processed",
      "anomalies": []
    }
  ]
}
```

| 필드 | 설명 |
|---|---|
| `file_hash` | 파일 바이트의 SHA-256. 재처리 차단 기준 |
| `kind` | §3의 4개 값. 매핑 실패 시 `null`, `status = "rejected"` |
| `header_row` / `data_start_row` | 접수 단계가 확인한 실제 행 번호 |
| `status` | `processed` \| `skipped_duplicate` \| `rejected` |
| `anomalies` | 접수 단계에서 발견한 이상 항목 배열. 없으면 빈 배열 |

`status = "rejected"`인 파일도 기록에 남긴다. 같은 파일이 다시 올라올 때 같은 판정을 반복하지 않기 위해서다.

---

## 8. transactions.json

**구현 매핑**: `webapp/prisma/schema.prisma`의 `Transaction` 테이블로 구현돼 있다. 필드 정의는
아래가 그대로 기준이고, 저장소만 JSON 파일 대신 Postgres 테이블이다. append-only 원칙(아래)은
`Transaction` 테이블에도 그대로 적용된다 — update/delete 없이 insert만 한다.

```json
{
  "version": 1,
  "transactions": [
    {
      "tx_key": "sha256:8c21…",
      "seq": 1,
      "kind": "매출-세금계산서",
      "side": "매출",
      "date": "2026-05-29",
      "month": "2026-05",
      "place": "(주)성일건설",
      "amount": 42350000,
      "proj": "2025 항만시설 유지보수공사",
      "use": "매출",
      "content": "한림,애월,성산 방충재교체 외",
      "evidence": "세계",
      "payer": null,
      "settle_date": "2026-06-11",
      "settle_method": "계좌",
      "source_file": "매출_세금계산서_2026년1-5월.xls",
      "source_row": 2,
      "file_hash": "sha256:3f9a…",
      "ingested_at": "2026-08-06T14:20:11+09:00"
    }
  ]
}
```

- `transactions`는 append-only로 다룬다. 기존 레코드를 수정하거나 삭제하지 않는다.
- classify의 확정/제외/수정 결과는 이 파일을 고치지 않고 별도 결과로 남긴다. 원본 거래는 항상 원문 상태로 보존된다.

---

## 9. 성공 기준

- [ ] `Transaction`/`ProcessedFile` 테이블(webapp 구현체 기준)이 생성되고 채워진다
- [ ] `Transaction`의 모든 레코드가 `tx_key`, `seq`, `kind`, `date`, `source_row`를 가진다
- [ ] 같은 파일을 두 번 업로드하면 두 번째는 `status = "skipped_duplicate"`이고 `tx_added = 0`이다
- [ ] 2026-02-12 아시아나항공 85,400원 3건이 `seq` 1/2/3으로 **모두** 누적된다(합계 256,200원)
- [ ] 2026-04-08 `창조이엔지` 21,489,886원이 매출/매입 **2건**으로 누적된다(하나로 합쳐지지 않는다)
- [ ] 2026-01 원천세 금액 공백 1건이 `amount = null`로 누적되고, 0으로 채워지지 않는다
- [ ] `tx_added + tx_skipped_duplicate`가 파일의 데이터행 수와 일치한다
- [ ] 매출 11건 전부에 `settle_date`가 채워진다
