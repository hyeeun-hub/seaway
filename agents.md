# agents.md - ERP Flow Harness 운영 지침

이 문서는 erp-flow-harness에서 에이전트가 어떤 순서로 움직이고, 어디까지 자동으로 처리하며, 사람 개입은 어디서 허용되는지 정리한 전체 운영 지침이다.  
전체 흐름은 오케스트레이터가 처음부터 끝까지 이어주는 실행 책임으로 운영하며, 파일 업로드 후 자동으로 이어져 마지막에 최종 결과를 정리하도록 운영한다.  
노출 범위는 `SPEC.md` §3 결과 노출 정책을, 중복 판정과 상태 파일은 `state-schema.md`를 기준으로 한다.

## 1. 목적
- ERP 파일 업로드부터 파일 접수, 분석/계산, 검수/분류, 결과 생성/조회, 캘린더/챗봇/요약, 자동 검사/검증, 최종 결과 확인까지 이어지는 흐름을 운영한다.
- 반복 작업의 핵심을 자동화하고, 예외/이상은 버리지 않고 검수·조치로 이어지게 한다.
- 사용자는 중간 처리 과정을 보지 않고, 최종 결과에서 결과를 확인하고 다음 액션을 판단할 수 있게 한다.

## 2. 전체 흐름
1. 파일 업로드
2. file-input-agent가 파일을 접수·저장·정리하고 업로드 시점과 중복 여부를 기록
3. analyze-agent가 파싱·정규화·프로젝트 매칭·손익 계산·월별 집계·일반관리비 분류를 수행하고, 거래 누적 보관/파일 처리 기록을 남김
4. classify-agent가 불확실/확인 필요 거래를 분리하고 확정/제외/수정/보류를 구분
5. query-export-agent가 프로젝트별 손익, 월별 손익, 월간 리포트 조회/생성용 결과를 준비하고, 웹 결과물 파일 생성용 정보도 준비
6. calendar-chatbot-agent가 회수 캘린더, 챗봇 응답, 실행 요약/상태를 정리
7. verify-agent가 각 단계와 최종 결과에 대해 자동 검사/검증을 수행
8. final-assembly(오케스트레이터 직접 수행)가 앞 단계 결과를 모아 최종 결과를 한 번에 정리하고, 웹에서 볼 수 있는 결과물 파일도 함께 첨부처럼 보여줌
9. notion-export-agent가 같은 최종 결과 요약을 고정 노션 페이지("씨웨이테크 대시보드")에 덮어써 동기화

## 3. 자동 실행 원칙
- 사용자가 ERP 파일을 업로드하고 분석 요청을 하면 오케스트레이터가 전체 흐름을 자동으로 시작해 끝까지 이어간다.
- 처음 프롬프트 이후에는 사용자에게 다시 묻거나 재요청하지 않고, 앞 단계가 끝나면 바로 다음 단계로 이어서 실행한다.
- 각 단계는 별도 확인 버튼 없이 자동으로 이어받는다.
- file-input이 끝나면 analyze로, analyze가 끝나면 중간 확인 없이 바로 classify로, classify가 끝나면 query-export로, query-export가 끝나면 calendar-chatbot으로, calendar-chatbot이 끝나면 verify로, verify가 끝나면 final-assembly로 넘긴다.
- analyze 결과는 이번에 들어온 파일만이 아니라 누적 거래 기준으로 정리한다.
- analyze 결과에는 거래 누적 보관 결과(추가/스킵/전체 누적 건수)와 파일 처리 기록(처리 완료 파일, 처리 시점, 파일 해시)도 포함한다.
- query-export 결과는 누적 거래 기준으로 만들고, 웹 결과물 파일 생성용 정보도 준비할 수 있다.
- final-assembly는 앞 단계 결과를 모두 모아 최종 결과 형식으로 정리하고, 웹에서 볼 수 있는 결과물 파일도 함께 첨부처럼 보여준다.
- 웹 결과물 파일은 브라우저에서 바로 열리는 한 개 파일로 만든다.
- 웹 결과물 파일은 매번 새 시점 파일명으로 새로 만든다.
- final-assembly가 끝나면 notion-export-agent로 이어져, 같은 요약을 고정 노션 페이지에 덮어써 갱신한다. 검색으로 찾지 못하면 재검색을 한 번 더 하고, 그래도 없으면 새 페이지를 만들어 이어간다. 동일 제목이 2건 이상 매칭되는 경우만 이상 항목으로 남긴다.
- 중간 처리 결과는 다음 단계가 이어서 사용할 수 있게 남기되, 단계별로 결과를 따로 저장했다가 다시 읽는 방식보다 순서대로 넘겨서 바로 이어지게 처리한다. 단, 거래 누적 보관과 파일 처리 기록은 실행 사이에 유지돼야 하므로 상태 파일에 저장한다(`state-schema.md`).
- 각 단계의 노출 범위는 `SPEC.md` §3 결과 노출 정책을 따른다.
- final-assembly에서는 앞 단계 결과를 모아 최종 결과를 한 번에 보기 좋게 정리한다.
- 한 번의 업로드+명령으로 끝까지 이어지게 만든다.

## 4. 자동 처리 원칙
- 딱 맞는 규칙 조건이 충족되면 자동 처리한다.
- 조건이 애매하면 자동 처리하지 않고 사람 확인 대상으로 남긴다.
- 검수 필요 목록이 나오면 검수 목록으로 이동했다고 표시하고, 이후 사용자가 그 목록으로 가서 확인/확정할 수 있게 연결한다.
- 검증에서 이상이 발견되면 결과를 그대로 끝내지 않고 검수/분류 단계로 넘겨 처리할 수 있게 연결한다.

## 5. 사람 개입이 허용되는 지점
- 검수창에서 대표가 확인/확정하는 단계
- 챗봇 질의, 캘린더 수정 입력처럼 사용자 입력이 발생하는 지점
- 검증 결과 안내에 따라 재시도/재입력/확인이 필요한 경우

## 6. 중복 처리 원칙

키 구성과 정규화 규칙의 기준 문서는 `state-schema.md` §4다. 여기서는 운영 원칙만 적는다.

- 중복 차단은 2단 방어선으로 한다. **1차는 파일 해시**(`processed_files.json`), **2차는 거래 식별자**(`(tx_key, seq)`).
- 중복 판단은 이번에 들어온 거래끼리 비교하는 것이 아니라, 누적 보관된 거래 전체와 비교한다.
- 거래 식별자에 **`source_file`을 넣지 않는다.** 재추출로 파일명이 바뀌면(`_1-5월` → `_1-6월`) 중복 제외가 동작하지 않기 때문이다.
- 거래 식별자에 **출현 순번 `seq`를 반드시 포함한다.** 필드 값이 전부 동일한 정상 거래가 실재하므로(출장자 3명분 항공권 등), `seq` 없이는 정상 반복이 중복으로 삼켜진다.
- 같은 식별자가 이미 누적돼 있으면 추가하지 않고 스킵한다.
- 같은 월 데이터가 나중에 추가로 들어오면, 중복은 제외하고 나머지 반영 후 재생성한다.
- 매 실행마다 추가/스킵/전체 누적 건수를 남기고, `추가 + 스킵 = 파일 데이터행 수`가 성립하는지 verify가 검사한다.

## 7. 검증 원칙
- 자동 검사/검증은 마지막 한 번만 하지 않고, 파일 접수 후/분석 후/검수 후/결과 생성 후/최종 결과 생성 후 등 중간에 분산 배치한다.
- 검사 결과에 따라 중단/경고/재시도/안내 중 적절한 조치를 선택한다.
- 검사 실패 시 원인과 조치를 남기고, 필요한 경우 재시도와 안내를 유도한다.
- 검사 결과는 최종 결과에 포함해 함께 보이게 하되, 이상이 발견되면 진행 중이라도 그 시점에 짧게 표시한다(`SPEC.md` §3).

## 8. 결과 확인 원칙
- 최종 결과는 결과 확인 화면, 겉보기 결과 정리, 다음 액션 안내로 정리한다.
- 중간 단계의 상세 산출물은 노출하지 않고, 최종 결과에서 프로젝트별 손익, 월별 손익, 월간 리포트, 검수 대상/처리 방향, 회수 캘린더/실행 요약, 검사 결과/이상 항목/재시도·안내 필요 여부, 다음 액션 안내를 함께 확인할 수 있게 한다.
- 최종 결과에는 웹에서 볼 수 있는 결과물 파일도 함께 나온다.
- 웹 결과물 파일은 브라우저에서 바로 열리는 한 개 파일로 만든다.
- 웹 결과물 파일은 매번 새 시점 파일명으로 새로 만든다(`out/erp-flow-result-<YYYYMMDD-HHMM>.html`).
- 저장 위치·인코딩·챗봇 연동 고정 스크립트 등 HTML 생성 세부 규칙은 `workflow.md` §7이 기준이다.
- 같은 요약이 고정 노션 페이지("씨웨이테크 대시보드")에도 동기화된다. 새 페이지를 만들지 않고 페이지 본문을 덮어쓰며, 절차는 `notion-export-skill`이 기준이다.
- 웹 결과물 파일에는 상단 요약, 프로젝트별 손익, 월별 손익+월간 리포트, 일반관리비 분류 결과, 검수 필요 목록+대표 예시, 회수 캘린더/실행 요약, 검사 결과/이상 항목/원인/조치 안내, 다음 액션 안내, 기준 정보가 들어간다.
- 결과가 비어 있거나 줄어든 경우에도 단순 빈 화면이 아니라, 어떤 기준으로 조회했고 왜 그렇게 보이는지 확인할 수 있게 한다.

## 9. 재현 원칙
- 이 Harness 폴더만 보고도 전체 구조를 이해할 수 있어야 한다.
- 실제 데이터 파일, 역할표, 워크플로우, 상태 스키마, 훅/설정, 스킬/에이전트 문서가 함께 있어야 한다.
- 동일 조건에서 다시 실행했을 때도 같은 흐름으로 이어질 수 있게 정리한다.
- 업로드 후 첫 명령 이후 다시 묻지 않고 끝까지 이어지고, analyze가 끝나면 바로 classify로 넘어가며, final-assembly에서 앞 단계 결과를 모아 최종 결과를 한 번에 보여주는 흐름이 재현되어야 한다.
