# spec.md - ERP Flow Harness 프로젝트 명세

## 0. 문서 역할과 단일 진실 소스

이 Harness의 문서는 서로 내용이 겹친다. 같은 규칙이 여러 문서에 중복 기술되면 한쪽만 고쳐져 드리프트가 생기므로,
항목별로 **어느 문서가 기준인지**를 아래와 같이 고정한다. 기준 문서가 아닌 곳에서는 정의를 다시 쓰지 않고 참조한다.

| 항목 | 기준 문서 |
|---|---|
| 문제정의, 단계 정의, 검증 기준, 폴더 구성, 제출물 | `SPEC.md` (이 문서) |
| 결과 노출 정책 | `SPEC.md` §3 |
| 거래 정규화, 파일별 열 매핑, 중복 판정 키, 상태 파일 스키마, 회수 예정일 파싱 | `state-schema.md` |
| 에이전트별 역할·입출력·연결 | `role-table.md` |
| 실행 순서, 단계 간 연결, final-assembly | `orchestrator.md` |
| 단계별 흐름과 트리거 | `workflow.md` |
| 웹 화면·집계·챗봇 구현(실제 구현체) | `webapp/README.md` |
| 운영 지침(자동 처리 범위, 사람 개입 지점) | `agents.md` |
| 스킬별 절차·예시·성공 기준 | `skills/<name>/SKILL.md` |
| 에이전트 frontmatter·반환 계약·경계 | `agents/erp-flow-<name>-agent.md` |

---

## 1. 문제정의 및 활용성

### 해결하려는 반복 작업
- ERP 엑셀 파일을 받으면, 파일을 접수하고, 파싱·정규화하고, 프로젝트/현장 단위로 손익을 계산하고, 월별로 집계하고, 일반관리비를 분류하고, 확인 필요 거래를 검수하고, 결과를 조회/리포트로 준비하고, 회수 캘린더/챗봇 응답/실행 요약까지 이어지는 작업을 반복한다.
- 이 과정을 사람이 매번 따로 하면, 파일 정리, 프로젝트 매칭, 월별 집계, 분류, 검수 분리, 결과 정리에서 병목이 생긴다.

### 실제 업무 병목과의 연결
- 파일 입수 후에도 “어느 프로젝트인지”, “몇 월인지”, “중복인지”, “분류가 애매한지”, “확정할 수 있는지”를 사람 손으로 계속 확인해야 한다.
- 특히 같은 월 데이터가 나중에 추가로 들어오면, 중복을 제외하고 다시 정리해야 해서 반복 작업이 커진다.

### 결과물의 활용성
- 최종 결과는 프로젝트별 손익, 월별 손익, 월간 리포트, 검수 대상/처리 방향, 회수 캘린더/챗봇 응답/실행 요약, 검사 결과/이상 항목/재시도·안내 필요 여부로 정리된다.
- 사용자는 결과를 확인하고, 겉보기 정리와 다음 액션 안내를 볼 수 있다.
- 단순 내부 텍스트가 아니라, 조회/확인/후속 조치로 이어지는 형태로 활용한다.
- 업로드 후 자동으로 이어져 final-assembly에서 앞 단계 결과를 모아 최종 결과를 한 번에 보기 좋게 보여준다. 무엇을 보여주고 무엇을 감추는지는 §3의 결과 노출 정책을 따른다.

---

## 2. 전체 흐름

### 시작 조건
- ERP 파일 업로드와 분석 요청이 발생하면 워크플로우가 시작된다.
- 시작되면 오케스트레이터가 전체 흐름을 처음부터 끝까지 이어주는 실행 책임으로 운영한다.
- 처음 프롬프트 이후에는 사용자에게 다시 묻거나 재요청하지 않고, 앞 단계가 끝나면 바로 다음 단계로 이어서 실행해 최종 결과까지 보여준다.
- 특히 analyze가 끝나면 중간 확인 없이 바로 classify로 넘겨 검수 대상을 정리하고, 이후에도 query-export → calendar-chatbot → verify → final-assembly까지 이어서 결과를 만든다.

### 단계
1. 파일 업로드
2. 파일 입력/관리
3. 분석/계산
4. 검수/분류
5. 결과 생성/조회
6. 캘린더/챗봇/요약
7. 자동 검사/검증
8. 최종 결과 확인/겉보기

### 각 단계의 목적
- 파일 업로드: ERP 파일 업로드
- 파일 입력/관리: 업로드 파일 접수, 저장/정리, 중복 확인, 업로드 시점 기록
- 분석/계산: ERP 파싱, 정규화, 프로젝트 매칭, 손익 계산, 월별 집계, 일반관리비 분류
- 검수/분류: 불확실/확인 필요 거래 분리, 문제 유형/처리 방법 정리, 확정/제외/수정/보류 구분
- 결과 생성/조회: 프로젝트별 손익, 월별 손익, 월간 리포트 조회/생성용 준비
- 캘린더/챗봇/요약: 확정 계약 자동 반영, 예정 계약 관리, 챗봇 응답, 실행 요약/상태
- 자동 검사/검증: 단계별 자동 검사, 최종 무결성 확인, 이상 원인/조치 안내
- 최종 결과 확인/겉보기: 결과 확인, 겉보기 결과 정리, 다음 액션 안내

### 자동 검사/검증 배치
- 마지막 한 번만 하지 않는다.
- 업로드 직후, 분석 후, 검수 후, 결과 생성 후, 최종 결과 생성 후 등 흐름 중간에 분산 배치한다.
- 검사 결과에 따라 중단/경고/재시도/안내 중 적절한 조치를 선택한다.
- 검사 실패 시 결과를 그대로 끝내지 않고 원인과 조치를 남긴다.

### 결과 전달 방식
- 각 단계는 자기 결과를 다음 단계로 바로 넘겨서 이어지게 처리한다.
- 단계별 중간 확인이나 재요청 없이, 첫 명령 이후 끝까지 순차 실행으로 이어간다.
- 단계별 결과를 따로 저장했다가 다시 읽는 방식보다, 순서대로 넘겨서 final-assembly까지 한 번에 이어지게 하는 방향을 기본으로 한다.
- final-assembly에서는 앞 단계 결과를 모아 최종 결과를 한 번에 보기 좋게 정리한다.
- 각 단계의 노출 범위는 §3의 결과 노출 정책을 따른다.

---

## 3. 데이터 흐름의 기준 원칙

### 핵심 거래 항목
- ERP 엑셀은 파일마다 제목행, 헤더행, 데이터행 구조를 가진다.
- 공통 핵심 항목은 대체로 거래일자, 금액, 용도/계정 성격, 내용, 프로젝트/현장, 증빙/참조 정보, 기타 참고 열이다.

### 중복 제외 원칙
- 중복 차단은 **파일 단위(1차)와 거래 단위(2차)의 2단 방어선**으로 한다.
  - 1차: 파일 해시가 이미 처리 기록에 있으면 파싱하지 않고 스킵한다.
  - 2차: 해시가 다른 파일에 대해, 거래 식별자가 이미 누적돼 있으면 그 거래만 스킵한다.
- 거래 식별자는 `(tx_key, seq)`다. **필드 값만으로는 중복 판정이 불가능하다** — 실데이터에 모든 열이 완전히 동일한 정상 거래가 존재하기 때문이다(출장자 3명분 항공권, 왕복 교통비, 커피 2잔 개별 결제 등). 동일 필드 조합의 출현 순번(`seq`)을 식별자에 포함해야 정상 반복을 삼키지 않는다.
- 키 구성, 정규화 규칙, 실측 근거는 `state-schema.md` §4가 기준이다. 이 문서에서 키를 다시 정의하지 않는다.
- 같은 월 데이터가 나중에 추가로 들어오면, 중복은 제외하고 나머지 반영 후 재생성한다.
- 중복 판정 결과는 매 실행마다 추가/스킵/전체 누적 건수로 남기고 최종 결과에 포함한다.

### 결과 노출 정책

**이 절이 노출 정책의 단일 기준이다.** 다른 문서는 이 절을 참조하고 자체 정의를 두지 않는다.

| 구분 | 내용 |
|---|---|
| 노출 | 단계명 + 진행 상태 + 핵심 수치 1~2개. 단계당 최대 2줄 |
| 비노출 | 거래 단위 상세, 중간 계산 과정, 규칙 판정 근거, 단계별 전체 산출물 |
| 즉시 노출(예외) | 검증에서 이상이 발견되면 그 시점에 이상 유형과 조치 방향을 1~2줄로 표시 |
| 최종 노출 | final-assembly에서 §최종 결과 8개 항목을 웹 화면(webapp)에 정리 |

문서에서 말하는 "중간 결과 비노출"은 **상세 산출물을 노출하지 않는다**는 뜻이며, 진행 상황 표시를 금지하는 뜻이 아니다. 두 표현이 충돌하는 것처럼 보이면 이 표가 기준이다.

### 분류 원칙
- 딱 맞는 규칙 조건이 충족되면 자동 처리한다.
- 조건이 애매하면 자동 처리하지 않고 사람 확인 대상으로 남긴다.
- 검수창에서 대표가 확인·확정할 수 있게 검수 대상/문제 유형/처리 방향을 남긴다.
- 검수 필요 목록이 나오면 검수 목록으로 이동했다고 표시한다.

### 검증 원칙
- 이상이 있으면 결과를 그대로 끝내지 않고 검수/분류 단계로 넘겨 처리할 수 있게 연결한다.
- 검증 기준은 반복 실행해도 같은 기준으로 점검한다.
- 중간 검사 결과는 최종 결과에 포함한다.

---

## 4. 스킬 명세 요약

각 스킬은 `skills/<name>/SKILL.md`에 입출력·범위·예시·성공 기준을 상세히 적는다. 아래는 요약이다.
파일별 열 매핑, 거래 정규화, 중복 판정, 상태 파일 스키마는 스킬 문서가 아니라 `state-schema.md`가 기준이다.

### file-input-skill
- 목적: ERP 엑셀 파일 접수, 저장/정리, 중복 확인, 업로드 시점 기록
- 입력: 업로드 파일, 파일명, 업로드 시점, 파일 기본 메타정보
- 출력: 저장 위치, 처리 상태, 중복 여부, 업로드 시점, 분석용 파일 참조 정보, 이상 접수 시 이상 항목
- 실행 조건: 파일 업로드 발생 시 가장 먼저 실행
- 산출물: 파일 접수 기록, 분석/검증이 참조할 파일 참조 정보

### analyze-skill
- 목적: ERP 파싱, 정규화, 프로젝트 매칭, 손익 계산, 월별 집계, 일반관리비 분류
- 입력: 업로드 파일/참조 정보, 프로젝트 목록/규칙, 분석 설정/규칙, 업로드 시점
- 출력: 프로젝트별 손익, 월별 손익, 일반관리비 분류 결과, 분석 시점, 기준 파일 정보, 매칭 결과/신규 프로젝트 후보, 거래 누적 보관 결과, 파일 처리 기록, 이상/주의 항목
- 실행 조건: 파일 입력/관리 처리 완료 후 실행
- 산출물: 프로젝트별 손익, 월별 손익, 일반관리비 분류 결과, 분석 시점, 기준 파일 정보, 매칭 결과/신규 프로젝트 후보, 거래/파일 처리 기록 갱신(`Transaction`/`ProcessedFile` 테이블, `state-schema.md` §7·§8)

### classify-skill
- 목적: 불확실 거래 분리, 문제 유형/처리 방법 정리, 확정/제외/수정/보류 구분, 검수 목록 생성
- 입력: 분석된 거래 데이터, 매칭 결과, 금액/날짜/프로젝트 이상 정보, 분석 시점/기준 파일 정보, 규칙 처리 기준
- 출력: 검수 대상 목록, 거래별 문제 유형, 처리 방법, 확정/제외/수정 결과, 규칙 처리 항목 결과, 검수 목록 이동 표시
- 실행 조건: 분석/계산 결과 수신 후 실행
- 산출물: 검수 대상 목록, 거래별 문제 유형/처리 방법, 확정/제외/수정/보류 구분 결과, 규칙 처리 결과

### query-export-skill
- 목적: 프로젝트별 손익, 월별 손익 조회, 월간 리포트 생성/조회
- 입력: 조회 조건, 리포트 요청, 선택 월/시점, 분석/검수 결과
- 출력: 프로젝트별 손익 화면 데이터, 월별 손익 화면 데이터, 월간 리포트, 기준 월/합계/프로젝트별 표/전월 대비 증감/요약 문구/생성 시점/기준 정보, 화면 표시용 정보
- 실행 조건: 분석/검수 결과 준비 후 실행, 조회/생성 요청 시 동작
- 산출물: 프로젝트별 손익/월별 손익 조회용 데이터, 월간 리포트, 기준 정보 및 생성 시점

### calendar-chatbot-skill
- 목적: 회수 캘린더 관리, 챗봇 응답, 실행 요약/상태 제공
- 입력: 확정 계약 데이터, 예정 계약 데이터, 분석/검수 결과, 챗봇 질의, 사용자 캘린더 수정 입력
- 출력: 회수 캘린더 데이터, 캘린더 뷰/목록 뷰용 정보, 챗봇 응답, 실행 요약/상태
- 실행 조건: 분석/검수 결과 준비되면 실행
- 산출물: 회수 캘린더 데이터, 캘린더/목록 뷰 정보, 챗봇 응답, 실행 요약/상태

### verify-skill
- 목적: 파일/분석/검수/결과 단계 자동 검사, 최종 무결성 점검, 이상 시 검수 연결
- 입력: 파일 정보, 파일 상태, 분석 결과, 검수 결과, 최종 결과, 생성 시점/기준 정보
- 출력: 검사 결과, 이상 항목, 실패/이상 원인, 상태 요약, 재시도/안내 필요 여부, 필요 시 검수 연결 대상
- 실행 조건: 각 단계 종료 후, 최종 결과 생성 후 다시 실행
- 산출물: 단계별 검사 결과, 이상 항목 목록, 상태 요약, 재시도/안내 필요 여부

---

## 5. 에이전트 명세 요약

각 에이전트는 `agents/erp-flow-<name>-agent.md`에 역할·연결·개입 범위·트리거·최종 산출물을 상세히 적는다.
아래는 요약이며, **정식 역할표는 `role-table.md`가 기준이다.** 두 표가 어긋나면 `role-table.md`를 따른다.

| 에이전트 | 역할 | 사용 스킬 | 다음/협력 |
|---|---|---|---|
| file-input-agent | 파일 접수·저장·정리, 중복 확인, 업로드 시점 기록 | file-input-skill | 다음: analyze / 협력: verify |
| analyze-agent | 파싱·정규화·매칭·손익·월별 집계·일반관리비 분류, 누적 보관 | analyze-skill | 다음: classify / 협력: query-export, calendar-chatbot, verify |
| classify-agent | 확인 필요 거래 분리, 문제 유형/처리 방법, 확정·제외·수정·보류 | classify-skill | 다음: query-export / 협력: calendar-chatbot, verify |
| query-export-agent | 프로젝트별·월별 손익 조회, 월간 리포트, 웹 화면(webapp) 표시용 정보 | query-export-skill | 협력: calendar-chatbot, verify |
| calendar-chatbot-agent | 회수 캘린더, 챗봇 응답, 실행 요약/상태 | calendar-chatbot-skill | 협력: query-export, verify |
| verify-agent | 6개 지점 자동 검사, 최종 무결성, 이상 시 검수 연결 | verify-skill | 전체 흐름 중간/끝 |
| final-assembly | 최종 결과 8개 항목 조립, 웹 화면(webapp)에 정리 | — (오케스트레이터 직접 수행) | 흐름의 마지막 |

**입출력과 결과 전달 방식은 이 표에 중복 기술하지 않는다.** `role-table.md`를 본다.

---

## 6. 자동 검사/검증 기준

### 검증 지점(6개)과 예외 처리 원칙

자동 검사는 아래 6개 지점에서 반복 실행된다. `after_file_input` / `after_analyze` /
`after_classify` / `after_query_export` / `after_calendar_chatbot`은 실제 구현체
`webapp/lib/verify.ts`의 `point` 필드 값과 문자 그대로 일치한다 — 문서와 코드가 같은 이름을
쓰므로 어떤 검사가 어느 지점에서 도는지 코드에서 바로 확인할 수 있다.

`after_final_result`(최종 결과 생성 후 최종 무결성 점검)는 아래 "최종 무결성" 절의 개념적
지점이며, 현재 `lib/verify.ts` 코드에는 별도 `point` 값으로 분리돼 있지 않다 — 해당 검사들
(정합성 검산 A/B/C 등)은 코드상 `after_query_export`로 태그돼 있다. 문서와 코드가 이 지점만
어긋나 있음을 그대로 남긴다(검사 결과를 숨기지 않는다는 원칙과 같은 이유로, 문서 상태도
숨기지 않는다).

예외 처리 원칙: 파일 읽기 실패, 필수 열 누락, 파싱/정규화 이상, 집계 불일치, 검수 누락 의심,
결과 불일치가 발생하면 원인/조치/재시도·안내 필요 여부를 남기고, 검사 결과를 그대로 끝내지
않고 검수/분류 단계로 넘길 수 있게 연결한다(§7과 동일 원칙).

### 파일 접수 단계
- 파일 존재/읽힘 여부
- 파일 비어 있음 여부
- 데이터 시트/데이터행 유무
- 필수 열 존재 여부
- 중복 접수 의심 여부

### 분석/계산 단계
- 거래일자/금액/프로젝트/현장 등 형식 이상
- 날짜 누락, 금액 비숫자 등 파싱/정규화 이상
- 프로젝트 미매칭 과다
- 월별 집계 합계 불일치
- 일반관리비 분류 불가/보류 과다
- 중복 제외 결과 검증: `추가 + 스킵 = 파일 데이터행 수`가 성립하는지
- 동일 필드 거래의 `seq` 배정 누락으로 정상 반복이 삼켜졌는지

### 검수/분류 단계
- 검수 대상 과다
- 동일 문제 유형 반복과 규칙 처리 누락
- 확정/제외/수정 간 충돌 가능성
- 사람 확인 필요 건 잔존 여부

### 결과 생성/조회 단계
- 조회 조건 대비 결과 공백의 원인 구분
- 월간 리포트 기준 월과 반영 데이터 일치 여부
- 분석/검수 결과와의 일관성

### 최종 무결성
- 결승선 데이터 필수 항목 누락 여부
- 단계별 결과 간 정합성
- 이전 검사 이상 항목의 최종 결과 반영 여부

---

## 7. 예외/실패 처리

- 파일 읽기 실패, 필수 열 누락, 비어 있는 파일, 파싱/정규화 이상, 집계 불일치, 검수 누락 의심, 결과 불일치가 발생하면 원인을 남긴다.
- 단순 종료가 아니라, 재시도/안내 필요 여부 또는 검수/분류 단계 연결을 남긴다.
- 사람의 수동 파일 수정이나 값 임의 보정, 설계되지 않은 임시 조작은 기본 흐름에서 배제한다.

---

## 8. Harness 폴더 구성

이 Harness는 아래 구조로 재현할 수 있게 구성한다.

이 트리는 실제 저장소 구조와 일치한다(문서층 + 실제 구현체 `webapp/`).

```
erp-flow-harness/
  SPEC.md                  # 이 문서. 문제정의·단계·검증 기준·폴더 구성
  role-table.md            # 에이전트 역할표(정식)
  orchestrator.md          # 실행 순서와 단계 간 연결
  workflow.md              # 단계별 흐름과 트리거
  agents.md                # 운영 지침
  state-schema.md          # 정규화·열 매핑·중복 판정·상태 파일 스키마(구현 매핑 포함)
  skills/
    file-input-skill/SKILL.md
    analyze-skill/SKILL.md
    classify-skill/SKILL.md
    query-export-skill/SKILL.md
    calendar-chatbot-skill/SKILL.md
    verify-skill/SKILL.md
    skill-reviewer/SKILL.md      # 개발 보조. ERP 흐름에는 참여하지 않는다
  agents/
    erp-flow-file-input-agent.md
    erp-flow-analyze-agent.md
    erp-flow-classify-agent.md
    erp-flow-query-export-agent.md
    erp-flow-calendar-chatbot-agent.md
    erp-flow-verify-agent.md
  data/
    매출_세금계산서_*.xls
    매입(세금계산서)_*.xls
    매입(간이영수증)_*.xls
    매입(국세_지방세)_*.xls
    대출마스터_*.xlsx / 자산마스터_*.xlsx / 프로젝트마스터_*.xlsx / 급여대장_*.xls / 4대보험조회_*.xls
  hooks/
    subagent_log.sh           # 훅 이벤트를 logs/erp-flow.log에 기록
  .claude/
    settings.json             # 실제로 발동하는 Claude Code 훅 설정(SubagentStop 등)
  logs/
    erp-flow.log               # 훅이 기록하는 실행 로그(gitignore 대상, 실행 시 생성)
  webapp/                     # 실제 구현체 — Next.js 16 + Prisma + Postgres. 유일하게 실행되는 코드
    app/                      # 라우트: 대시보드/프로젝트별손익/월별추이/수주판단/일반관리비/캘린더/검수/메모/리포트/설정
    components/
    lib/
      pipeline/                # 파일 해시·종류 판별·파싱·정규화·txKey → file-input/analyze 대응
      classify.ts              # classify-skill 대응
      aggregate.ts             # query-export-skill 대응
      calendar.ts / gemini.ts  # calendar-chatbot-skill 대응
      verify.ts                # verify-skill 대응
    prisma/
      schema.prisma            # state-schema.md 필드 정의를 테이블로 구현
      migrations/
      seed.ts
    README.md                  # 웹 화면·집계·챗봇 구현 규칙의 기준 문서
```

**명명 규칙**

- 스킬은 `skills/<skill-name>/SKILL.md`. 디렉터리명이 frontmatter의 `name`과 일치해야 한다.
- 에이전트 파일은 `erp-flow-` 접두사를 붙인다. 서브에이전트 이름의 네임스페이스 역할을 하므로 생략하지 않는다.
- 에이전트 파일은 **frontmatter(`name`, `description`, `tools`, `model`) + 위임형 본문**으로 쓴다. `description`이 없으면 오케스트레이터가 자동 위임할 근거가 없으므로 반드시 채운다.
- 에이전트 본문에는 절차를 쓰지 않는다. 절차는 스킬 문서에 두고, 에이전트는 **정체성 / 사용 스킬 / 호출 시점 / 반환 계약 / 경계 / 진행 표시 / 다음**만 적는다. 같은 내용을 두 벌 쓰면 드리프트가 생긴다.
- `skill-reviewer`는 스킬 문서 품질을 셀프체크하는 개발 보조 스킬이며, ERP 흐름 6단계에 포함되지 않는다.

---

## 9. 재현 방법

- 이 Harness 폴더만 보고도 전체 구조를 이해할 수 있어야 한다.
- 데이터 폴더에 ERP 파일 또는 데모용 파일을 넣고, 업로드/접수부터 같은 순서로 실행하면 흐름이 재현된다.
- 검증 기준은 verify-agent/verify-skill이 담당하며, 반복 실행 시에도 같은 기준으로 점검한다.
- role-table.md, orchestrator.md, workflow.md, agents.md를 함께 보면 에이전트 역할, 흐름, 운영 원칙을 재현할 수 있다.
- state-schema.md를 함께 보면 파일 열 차이, 중복 판정, 상태 파일까지 동일하게 재현할 수 있다.
- 업로드 후 첫 명령 이후 다시 묻지 않고 끝까지 이어지고, analyze가 끝나면 바로 classify로 넘어가며, final-assembly에서 앞 단계 결과를 모아 최종 결과를 한 번에 보여주는 흐름을 재현할 수 있어야 한다.

---

## 10. 제출물 구성

제출물은 다음 세 가지로 정리한다.

- 반복작업 Agent PPT
- 데모 영상
- Harness ZIP

### 발표/설명 흐름
1. 문제
2. 설계
3. 자동
4. 검증
5. 효과

### 제출물에서 전달할 핵심
- 어떤 반복 작업을 해결하는지
- 어떻게 설계했는지
- 어떤 작업을 자동화했는지
- 결과를 어떻게 검증했는지
- 자동화로 어떤 효과가 발생했는지

---

## 11. 효과 요약

- ERP 파일 업로드를 시작으로 접수·분석·검수·결과·캘린더/챗봇·자동 검증까지 이어지는 반복 작업을 자동화로 묶는다.
- 딱 맞는 건 자동 처리하고, 애매한 건 사람 확인으로 넘겨 검수창에서 확정하게 한다.
- 검증 이상은 끝내지 않고 검수/분류로 넘겨 원인·조치·후속까지 이어지게 한다.
- 월별/중복 제외/프로젝트 매칭/일반관리비 분류/리포트 생성/조회 기준을 재사용 가능한 단위로 정리한다.
- 중간 단계의 상세 산출물은 노출하지 않고 진행 상황만 짧게 보여주며, 업로드 후 자동으로 이어져 최종 결과를 한 번에 정리한다(§3 결과 노출 정책).
