# role-table.md - ERP Flow Harness 역할표

ERP Flow Harness에서 각 에이전트의 역할, 사용 스킬, 입력, 출력, 결과 전달 방식, 연결 관계를 정리한 표다.  
전체 흐름 연결은 오케스트레이터가 끝까지 책임지는 실행 흐름으로 운영하며, 처음 프롬프트 이후에는 사용자에게 다시 묻거나 재요청하지 않고, 앞 단계가 끝나면 바로 다음 단계로 이어서 실행해 final-assembly에서 최종 결과를 한 번에 보여준다.  
노출 범위는 `SPEC.md` §3 결과 노출 정책을 따른다. 이 문서에서 노출 정책을 다시 정의하지 않는다.

## 에이전트 역할표

| 에이전트 | 역할 | 사용 스킬 | 주요 입력 | 주요 출력 | 결과 전달 방식 | 연결 |
|---|---|---|---|---|---|---|
| file-input-agent | ERP 파일 업로드 접수, 저장/정리, 중복 확인, 업로드 시점 기록 | file-input-skill | 업로드 파일, 파일명, 업로드 시점, 파일 기본 메타정보 | 저장 위치, 처리 상태, 중복 여부, 업로드 시점, 분석용 파일 참조 정보, `processed_files.json` 갱신 | 다음 에이전트로 자동 전달, 상세 산출물 비노출(진행 1줄 표시), 접수 이상은 검증/검수 쪽으로 연결 | 다음: analyze-agent / 협력: verify-agent |
| analyze-agent | ERP 파싱, 정규화, 프로젝트 매칭, 손익 계산, 월별 집계, 일반관리비 분류, 거래 누적 보관, 파일 처리 기록 | analyze-skill | 업로드 파일/참조 정보, 프로젝트 목록/규칙, 분석 설정/규칙, 업로드 시점 | 프로젝트별 손익, 월별 손익, 일반관리비 분류 결과, 분석 시점, 기준 파일 정보, 매칭 결과/신규 프로젝트 후보, 거래 누적 보관 결과, 파일 처리 기록 | 다음 에이전트로 자동 전달, 상세 산출물 비노출(진행 1줄 표시), 분석 이상은 검증/검수 쪽으로 연결 | 다음: classify-agent / 협력: query-export-agent, calendar-chatbot-agent, verify-agent |
| classify-agent | 불확실 거래 분리, 문제 유형/처리 방법 정리, 확정/제외/수정/보류 구분, 검수 목록 생성 | classify-skill | 분석된 거래 데이터, 매칭 결과, 금액/날짜/프로젝트 이상 정보, 분석 시점/기준 파일 정보 | 검수 대상 목록, 거래별 문제 유형, 처리 방법, 확정/제외/수정 결과, 규칙 처리 항목 결과, 검수 목록 이동 표시 | 다음 에이전트로 자동 전달, 상세 산출물 비노출(진행 1줄 표시), 검수 필요 건은 검수 목록으로 이동 표시 | 다음: query-export-agent / 협력: calendar-chatbot-agent, verify-agent |
| query-export-agent | 프로젝트별 손익, 월별 손익 조회, 월간 리포트 생성/조회, 최종 결과용 정리, 웹 결과물 파일 생성용 정보 준비 | query-export-skill | 조회 조건, 리포트 요청, 선택 월/시점, 분석/검수 결과 | 프로젝트별 손익 화면 데이터, 월별 손익 화면 데이터, 월간 리포트, 기준 월/합계/프로젝트별 표/전월 대비 증감/요약 문구/생성 시점/기준 정보, 웹 결과물 파일 생성용 정보 | 다음 에이전트로 자동 전달, 상세 산출물 비노출(진행 1줄 표시), 최종 결과에 포함 | 협력: calendar-chatbot-agent, verify-agent |
| calendar-chatbot-agent | 회수 캘린더 관리, 챗봇 응답, 실행 요약/상태 제공 | calendar-chatbot-skill | 확정 계약 데이터, 예정 계약 데이터, 분석/검수 결과, 챗봇 질의, 사용자 캘린더 수정 입력 | 회수 캘린더 데이터, 캘린더 뷰/목록 뷰용 정보, 챗봇 응답, 실행 요약/상태 | 다음 에이전트로 자동 전달, 상세 산출물 비노출(진행 1줄 표시), 최종 결과에 포함 | 협력: query-export-agent, verify-agent |
| verify-agent | 파일/분석/검수/결과 단계 자동 검사, 최종 무결성 점검, 이상 시 검수 연결 | verify-skill | 파일 정보, 파일 상태, 분석 결과, 검수 결과, 최종 결과, 생성 시점/기준 정보 | 검사 결과, 이상 항목, 실패/이상 원인, 상태 요약, 재시도/안내 필요 여부, 검수 연결 대상 정보 | 상세 검사 로그 비노출, 이상 발견 시 그 시점에 1~2줄 즉시 표시, 최종 결과에 포함 | 전체 흐름 6개 검사 지점(중간/끝) |
| **final-assembly** (오케스트레이터 직접 수행) | 앞 단계 결과를 모아 최종 결과 8개 항목으로 조립, 웹 결과물 파일 생성 | — (스킬 없음) | file-input / analyze / classify / query-export / calendar-chatbot / verify 전 단계 결과 | 최종 결과 8개 항목, 웹 결과물 파일(`out/erp-flow-result-<YYYYMMDD-HHMM>.html`), 다음 액션 안내 | 사용자에게 최종 노출. 단계별 세부 흔적은 길게 드러내지 않는다 | 이후 verify 최종 무결성 검사 결과를 반영해 마감. 다음: notion-export-agent |
| notion-export-agent | 최종 결과 요약을 고정 노션 페이지("씨웨이테크 대시보드")에 덮어써 동기화 | notion-export-skill | final-assembly 최종 결과 요약, 고정 페이지 식별 정보 | 동기화 상태(성공/실패), 갱신된 페이지 정보, 새 페이지 생성 여부, 동기화 시점, 실패 시 이상 항목 | 검색 1회 실패 시 재검색 1회, 그래도 없으면 새 페이지 생성. 동일 제목 2건 이상 매칭 시에만 이상으로 남김. 최종 결과에 동기화 상태 포함 | 흐름의 마지막. 다음 없음 |

## 책임 구분 원칙
- 각 에이전트는 자기 역할이 명확하고, 다른 에이전트와 중복되지 않게 한다.
- file-input-agent는 접수·정리·참조 정보까지, analyze-agent는 파싱·집계까지, classify-agent는 검수 분리·처리 구분까지 담당한다.
- query-export-agent는 조회/리포트 준비, calendar-chatbot-agent는 캘린더/챗봇/요약, verify-agent는 검사에 집중한다.
- 최종 결과 조립(final-assembly)은 **오케스트레이터가 직접 수행한다.** 별도 에이전트나 스킬을 두지 않는다(`orchestrator.md` §7).
- 6개 에이전트는 final-assembly에 자기 결과를 넘기는 것까지가 책임이며, 8개 항목 조립은 하지 않는다.
- notion-export-agent는 final-assembly가 조립한 결과를 고정 노션 페이지에 반영하는 것까지만 담당한다. 노션 인증 설정, 원본 거래 상세 업로드는 하지 않는다. 새 페이지 생성은 검색 1회 + 재검색 1회가 모두 실패했을 때만 예외적으로 허용된다.

## 오케스트레이터가 관리하는 협력 순서
- file-input-agent → analyze-agent → classify-agent → query-export-agent → calendar-chatbot-agent → verify-agent 순으로 자동으로 이어진다.
- 처음 명령 이후에는 다시 묻지 않고, analyze가 끝나면 중간 확인 없이 바로 classify로 넘겨 검수 대상을 정리한다.
- classify가 끝나면 query-export, calendar-chatbot, verify를 차례로 이어 실행하고, final-assembly가 최종 결과를 한 번에 정리한다.
- final-assembly가 끝나면 notion-export-agent가 같은 요약을 고정 노션 페이지에 동기화하며, 이것이 흐름의 마지막 연결이다.

## 결과 노출 원칙

노출/비노출 기준은 `SPEC.md` §3 결과 노출 정책이 유일한 기준이다. 요약하면 **진행 상황은 노출, 상세 산출물은 비노출**이다.
아래는 최종 결과에 무엇이 담기는지에 대한 규정이다.

- 각 단계는 결과를 단계별 저장/읽기로 넘기기보다, 순서대로 바로 이어받아 처리하는 방식으로 전달한다. 단, 거래 누적 보관과 파일 처리 기록은 실행 사이에 유지돼야 하므로 `state-schema.md`가 정의한 상태 파일에 저장한다.
- 최종 결과에는 파일 접수 요약, 프로젝트별 손익, 월별 손익, 월간 리포트, 일반관리비 분류 결과, 검수 대상/처리 방향, 회수 캘린더/실행 요약, 검사 결과/이상 항목/재시도·안내 필요 여부, 다음 액션 안내를 포함한다.
- 최종 결과에는 웹에서 볼 수 있는 결과물 파일도 함께 나온다.
- 웹 결과물 파일은 브라우저에서 바로 열리는 한 개 파일로 만든다.
- 웹 결과물 파일은 매번 새 시점 파일명으로 새로 만든다.
- 웹 결과물 파일에는 상단 요약, 프로젝트별 손익, 월별 손익+월간 리포트, 일반관리비 분류 결과, 검수 필요 목록+대표 예시, 회수 캘린더/실행 요약, 검사 결과/이상 항목/원인/조치 안내, 다음 액션 안내, 기준 정보가 들어간다.
- 검수 필요 목록이 있으면 검수 목록으로 이동했다고 표시하고, 대표 예시도 함께 보이게 한다.
- 검증 이상이 있으면 원인/조치/재시도·안내 필요 여부를 최종 결과에 함께 포함한다.
- final-assembly에서는 앞 단계 결과를 모아 최종 결과를 한 번에 보기 좋게 정리한다.
- final-assembly 이후 notion-export-agent의 동기화 상태(성공/실패)도 최종 결과에 함께 포함한다. 실패해도 다른 결과 출력은 그대로 진행한다.
